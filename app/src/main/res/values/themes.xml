<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.THECHEF" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/uber_eats_green</item>
        <item name="colorPrimaryContainer">@color/uber_eats_green</item>
        <item name="colorOnPrimary">@android:color/white</item> <!-- Text on primary color (optional) -->
    </style>

    <style name="Theme.THECHEF" parent="Base.Theme.THECHEF" />
</resources>
